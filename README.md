# ROS2 Keyboard
The `keyboard` node provides functionality for reading and publishing active keystrokes with ROS2. The node actively listens for keystrokes from the terminal and immediately publishes them to the `keystroke` topic.
